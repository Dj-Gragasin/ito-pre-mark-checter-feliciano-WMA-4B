# Production Environment Configuration Template
# Copy this file to your production server and update with actual values

# ============================================
# DATABASE CONFIGURATION (PostgreSQL)
# ============================================
DB_HOST=your-postgresql-host.render.internal
DB_PORT=5432
DB_USER=your-postgres-user
DB_PASSWORD=your-secure-database-password
DB_NAME=activecore

# ============================================
# JWT AUTHENTICATION
# ============================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-32-plus-character-secret-here

# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=3002
NODE_ENV=production

# Frontend URL for CORS
FRONTEND_URL=https://your-domain.com
ALLOWED_ORIGINS=https://your-domain.com,https://app.your-domain.com

# ============================================
# PAYPAL CONFIGURATION (Live Credentials)
# ============================================
PAYPAL_CLIENT_ID=your-live-client-id
PAYPAL_CLIENT_SECRET=your-live-client-secret
PAYPAL_MODE=live

# ============================================
# EMAIL CONFIGURATION (Optional)
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password
FROM_EMAIL=noreply@your-domain.com
INACTIVE_NOTIFY_DAYS=3

# ============================================
# OPENAI CONFIGURATION (Optional)
# ============================================
# Leave blank if meal planning AI is not needed
OPENAI_API_KEY=your-openai-api-key

# ============================================
# ERROR TRACKING (Sentry)
# ============================================
# Get from https://sentry.io - only activates if set
SENTRY_DSN=https://your-key@sentry.io/project-id

# ============================================
# LOGGING
# ============================================
# Logs will be written to: logs/error.log and logs/combined.log
# Set log level (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info
