# Backend Environment Variables Template
# Copy as .env for local dev. For Render, set these in the Render dashboard.

# ==================== RUNTIME ====================
NODE_ENV=development

# If running behind a reverse proxy (Render, Nginx, Cloudflare), enable this so req.ip reflects the real client.
TRUST_PROXY=0

# ==================== RATE LIMITING ====================
# Login: attempts per 15 minutes per IP
AUTH_RATE_LIMIT_MAX=20
# General API: requests per minute per IP
GENERAL_RATE_LIMIT_MAX=300
# Registration: attempts per hour per IP
REGISTER_RATE_LIMIT_MAX=10

# NOTE (Render): do NOT set PORT manually. Render injects PORT automatically.
# PORT=3002

# ==================== DATABASE (PostgreSQL) ====================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=activecore

# ==================== AUTH ====================
# Required. Must be at least 32 characters.
JWT_SECRET=replace-with-a-long-random-secret-at-least-32-chars

# ==================== CORS (Production) ====================
# Comma-separated list of allowed origins.
# If ALLOWED_ORIGINS is not set, the backend falls back to FRONTEND_URL.
ALLOWED_ORIGINS=http://localhost:8100
FRONTEND_URL=http://localhost:8100

# Allow ngrok.io for testing (set to 'true' to enable)
ALLOW_NGROK=false

# ==================== PAYPAL ====================
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_MODE=sandbox

# ==================== OPENAI (Optional) ====================
# Leave empty to disable AI features gracefully.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# ==================== EMAIL (Optional) ====================
# Used for notifications / emails.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=

# ==================== APP URL ====================
# Public frontend URL used in links.
APP_URL=http://localhost:3000

# ==================== NOTIFICATIONS ====================
INACTIVE_NOTIFY_DAYS=3

# ==================== MONITORING (Optional) ====================
SENTRY_DSN=
