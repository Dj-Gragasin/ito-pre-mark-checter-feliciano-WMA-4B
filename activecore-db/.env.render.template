NODE_ENV=production

# Behind Render's proxy/load balancer, enable this so req.ip uses the real client IP.
TRUST_PROXY=1

# Rate limiting (tune as needed)
# Login: attempts per 15 minutes per IP
AUTH_RATE_LIMIT_MAX=20
# General API: requests per minute per IP
GENERAL_RATE_LIMIT_MAX=300
# Registration: attempts per hour per IP
REGISTER_RATE_LIMIT_MAX=10

# Render Environment Variables (Backend)
# Put real values in Render -> Service -> Environment.
# Keep this file SECRET-FREE (safe to commit).

# Database (Render Postgres)
DB_HOST=dpg-xxxxxxxxxxxxxxxxxxxx-a.singapore-postgres.render.com
DB_PORT=5432
DB_USER=activecore_user
DB_PASSWORD=REPLACE_ME
DB_NAME=activecore

# Auth (Required)
JWT_SECRET=REPLACE_WITH_A_LONG_RANDOM_SECRET_AT_LEAST_32_CHARS

# CORS (Required)
ALLOWED_ORIGINS=https://activecore.vercel.app
# FRONTEND_URL is used only as a fallback if ALLOWED_ORIGINS is missing
FRONTEND_URL=https://activecore.vercel.app

# Links in email / redirects
APP_URL=https://activecore.vercel.app

# Email (Optional)
# If you're not sending emails yet, you can omit these entirely in Render.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=
INACTIVE_NOTIFY_DAYS=3

# Monitoring (Optional)
SENTRY_DSN=

# AI (Optional)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
